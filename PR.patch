From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: GPT-5 Thinking <dev@assistant.local>
Date: Sun, 21 Sep 2025 13:00:00 +0200
Subject: [feat] Minimal Godot 4 gacha skeleton (autoloads, data, UI:
 MainMenu→Gacha→CardGrid)

---
 .gitignore                                  |  10 +
 project.godot                               |  46 ++++
 autoloads/Database.gd                       |  62 +++++
 autoloads/GameState.gd                      |  41 +++
 autoloads/Gacha.gd                          | 128 +++++++++
 autoloads/RNG.gd                            |  18 ++
 autoloads/SaveManager.gd                    |  35 +++
 data/banners.json                           |  26 ++
 data/characters.json                        | 118 ++++++++
 data/missions.json                          |  28 ++
 data/rates.json                             |  11 +
 scenes/ui/CardGrid.gd                       |  29 ++
 scenes/ui/CardGrid.tscn                     |  65 +++++
 scenes/ui/GachaView.gd                      |  63 +++++
 scenes/ui/GachaView.tscn                    | 133 ++++++++++
 scenes/ui/MainMenu.gd                       |  25 ++
 scenes/ui/MainMenu.tscn                     |  78 ++++++
 scripts/BattleSimulator.gd                  | 103 ++++++++
 scripts/MissionResolver.gd                  |  36 +++
 20 files changed, 1035 insertions(+)
 create mode 100644 .gitignore
 create mode 100644 project.godot
 create mode 100644 autoloads/Database.gd
 create mode 100644 autoloads/GameState.gd
 create mode 100644 autoloads/Gacha.gd
 create mode 100644 autoloads/RNG.gd
 create mode 100644 autoloads/SaveManager.gd
 create mode 100644 data/banners.json
 create mode 100644 data/characters.json
 create mode 100644 data/missions.json
 create mode 100644 data/rates.json
 create mode 100644 scenes/ui/CardGrid.gd
 create mode 100644 scenes/ui/CardGrid.tscn
 create mode 100644 scenes/ui/GachaView.gd
 create mode 100644 scenes/ui/GachaView.tscn
 create mode 100644 scenes/ui/MainMenu.gd
 create mode 100644 scenes/ui/MainMenu.tscn
 create mode 100644 scripts/BattleSimulator.gd
 create mode 100644 scripts/MissionResolver.gd

diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,10 @@
+# Godot
+.import/
+export_presets.cfg
+*.translation
+
+# System
+.DS_Store
+Thumbs.db
+
+# IDE
+.vscode/
diff --git a/project.godot b/project.godot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/project.godot
@@ -0,0 +1,46 @@
+; Engine configuration file.
+; It's best edited using the editor UI and not directly,
+; since the parameters that go here are not all obvious.
+
+config_version=5
+
+[application]
+config/name="Divine Gacha (Minimal)"
+run/main_scene="res://scenes/ui/MainMenu.tscn"
+config/features=PackedStringArray("4.0", "GL Compatibility")
+
+[display]
+window/size/viewport_width=1280
+window/size/viewport_height=720
+window/stretch/mode="canvas_items"
+window/stretch/aspect="expand"
+
+[autoload]
+RNG="*res://autoloads/RNG.gd"
+DB="*res://autoloads/Database.gd"
+Gacha="*res://autoloads/Gacha.gd"
+GameState="*res://autoloads/GameState.gd"
+SaveManager="*res://autoloads/SaveManager.gd"
+
+[rendering]
+textures/vram_compression/import_etc2_astc=true
diff --git a/autoloads/Database.gd b/autoloads/Database.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/autoloads/Database.gd
@@ -0,0 +1,62 @@
+extends Node
+
+var chars:Dictionary = {}
+var banners:Array = []
+var rates:Dictionary = {}
+var missions:Array = []
+
+func _ready() -> void:
+    chars = _load_json("res://data/characters.json")
+    banners = _load_json("res://data/banners.json")
+    rates = _load_json("res://data/rates.json")
+    missions = _load_json("res://data/missions.json")
+
+func _load_json(path:String):
+    var f := FileAccess.open(path, FileAccess.READ)
+    if f == null:
+        push_error("Cannot open JSON: %s" % path)
+        return {}
+    var txt := f.get_as_text()
+    f.close()
+    var parsed = JSON.parse_string(txt)
+    if typeof(parsed) == TYPE_NIL:
+        push_error("Invalid JSON: %s" % path)
+        return {}
+    return parsed
+
+func get_banner(id:String) -> Dictionary:
+    for b in banners:
+        if b.get("id","") == id:
+            return b
+    return {}
+
+func pool_for_banner(banner:Dictionary) -> Array:
+    var pool:Array = []
+    for id in chars.keys():
+        var ch:Dictionary = chars[id]
+        if not ch.get("tags", []).has("female_only"):
+            continue
+        var ok := true
+        if banner.has("pool_tags"):
+            for t in banner.pool_tags:
+                if t == "female_only":
+                    continue
+                if not ch.get("tags", []).has(t) and ch.get("series","") != t:
+                    ok = false
+                    break
+        if ok and banner.has("exclude_tags"):
+            for t in banner.exclude_tags:
+                if ch.get("tags", []).has(t):
+                    ok = false
+                    break
+        if ok:
+            pool.append(ch)
+    return pool
diff --git a/autoloads/GameState.gd b/autoloads/GameState.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/autoloads/GameState.gd
@@ -0,0 +1,41 @@
+extends Node
+
+var inventory := {} # char_id -> { count:int, level:int, asc:int, chips:Array }
+var teams := { "TeamA": [] }
+var god_cards := []
+var currencies := { "gems": 3000, "gold": 0 }
+
+func add_character(char_id:String) -> void:
+    if not inventory.has(char_id):
+        inventory[char_id] = { "count": 1, "level": 1, "asc": 0, "chips": [] }
+    else:
+        inventory[char_id].count += 1
+
+func apply_pull_results(results:Array) -> void:
+    for r in results:
+        add_character(r.id)
+
+func team_power(ids:Array) -> int:
+    var power := 0.0
+    for cid in ids:
+        if not DB.chars.has(cid): continue
+        var ch := DB.chars[cid]
+        var s := ch.base_stats
+        power += s.atk*2.0 + s.hp*0.5 + s.def*1.2 + s.spd*1.5
+    return int(power)
diff --git a/autoloads/Gacha.gd b/autoloads/Gacha.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/autoloads/Gacha.gd
@@ -0,0 +1,128 @@
+extends Node
+
+const RARITIES := ["R","SR","SSR"]
+
+var pity_counter := 0
+var currency := { "gems": 0, "tickets": 0 }
+
+func pull_once(banner_id:String) -> Dictionary:
+    var banner := DB.get_banner(banner_id)
+    var rarity := _roll_rarity()
+    pity_counter += 1
+    if pity_counter >= int(DB.rates.hard_pity):
+        rarity = "SSR"
+    if rarity == "SSR":
+        pity_counter = 0
+
+    var pool := []
+    for ch in DB.pool_for_banner(banner):
+        if ch.rarity == rarity:
+            pool.append(ch)
+    if pool.is_empty():
+        push_error("Pool empty for rarity %s" % rarity)
+        return {}
+
+    # rate-up
+    if rarity == "SSR" and banner.has("rate_up") and RNG.randf() < float(DB.rates.rate_up.on_banner):
+        var ru_ids:Array = banner.rate_up
+        var pik_id := ru_ids[RNG.randi_range(0, ru_ids.size()-1)]
+        return { "rarity": rarity, "id": pik_id }
+
+    var pick := pool[RNG.randi_range(0, pool.size()-1)]
+    return { "rarity": rarity, "id": pick.id }
+
+func pull_x10(banner_id:String) -> Array:
+    var res:Array = []
+    var has_sr_plus := false
+    for i in 10:
+        var p := pull_once(banner_id)
+        if p.is_empty(): continue
+        if p.rarity != "R": has_sr_plus = true
+        res.append(p)
+    if not has_sr_plus:
+        res[res.size()-1] = _force_min_rarity(banner_id, "SR")
+    return res
+
+func _roll_rarity() -> String:
+    var base := DB.rates.base
+    var roll := RNG.randf()
+    var ssr_base := float(base.SSR)
+    var sp := DB.rates.soft_pity
+    if pity_counter >= int(sp.start):
+        var steps := pity_counter - int(sp.start)
+        ssr_base = min(ssr_base + steps * float(sp.step), float(sp.cap))
+    if roll < ssr_base:
+        return "SSR"
+    elif roll < ssr_base + float(base.SR):
+        return "SR"
+    return "R"
+
+func _force_min_rarity(banner_id:String, rarity:String) -> Dictionary:
+    var banner := DB.get_banner(banner_id)
+    var pool := []
+    for ch in DB.pool_for_banner(banner):
+        if ch.rarity == rarity:
+            pool.append(ch)
+    var pick := pool[RNG.randi_range(0, pool.size()-1)]
+    return { "rarity": rarity, "id": pick.id }
diff --git a/autoloads/RNG.gd b/autoloads/RNG.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/autoloads/RNG.gd
@@ -0,0 +1,18 @@
+extends Node
+
+var _rng := RandomNumberGenerator.new()
+
+func _ready() -> void:
+    _rng.randomize()
+
+func randf() -> float:
+    return _rng.randf()
+
+func randi_range(a:int, b:int) -> int:
+    return _rng.randi_range(a,b)
diff --git a/autoloads/SaveManager.gd b/autoloads/SaveManager.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/autoloads/SaveManager.gd
@@ -0,0 +1,35 @@
+extends Node
+
+const SAVE_PATH := "user://save.json"
+
+func save_all() -> void:
+    var data = {
+        "inventory": GameState.inventory,
+        "teams": GameState.teams,
+        "currencies": GameState.currencies,
+        "pity": Gacha.pity_counter
+    }
+    var f := FileAccess.open(SAVE_PATH, FileAccess.WRITE)
+    if f:
+        f.store_string(JSON.stringify(data))
+        f.close()
+
+func load_all() -> void:
+    if not FileAccess.file_exists(SAVE_PATH): return
+    var f := FileAccess.open(SAVE_PATH, FileAccess.READ)
+    if f == null: return
+    var txt := f.get_as_text()
+    f.close()
+    var d := JSON.parse_string(txt)
+    if typeof(d) != TYPE_DICTIONARY: return
+    GameState.inventory = d.get("inventory", {})
+    GameState.teams = d.get("teams", { "TeamA": [] })
+    GameState.currencies = d.get("currencies", { "gems": 0, "gold": 0 })
+    Gacha.pity_counter = int(d.get("pity", 0))
diff --git a/data/banners.json b/data/banners.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/data/banners.json
@@ -0,0 +1,26 @@
+[
+  {
+    "id": "BNR_MULTIVERSE_MIX",
+    "name": "Cross-Realm Heroines",
+    "pool_tags": ["female_only"],
+    "exclude_tags": [],
+    "rate_up": ["AZRYNN_SSR"]
+  },
+  {
+    "id": "BNR_DANLIKE_GODDESSES",
+    "name": "Hearth & Starlight",
+    "pool_tags": ["female_only","Dan-like"],
+    "exclude_tags": [],
+    "rate_up": ["HESTIA_SSR","FREYA_SSR"]
+  }
+]
diff --git a/data/characters.json b/data/characters.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/data/characters.json
@@ -0,0 +1,118 @@
+{
+  "HESTIA_SSR": {
+    "id": "HESTIA_SSR",
+    "name": "Hestia (inspired)",
+    "rarity": "SSR",
+    "series": "Dan-like",
+    "tags": ["goddess","support","female_only"],
+    "path": "Support",
+    "base_stats": { "atk": 90, "hp": 900, "def": 70, "spd": 102, "crit": 0.10 },
+    "skills": ["HESTIA_BLESS","WARM_HEARTH","DIVINE_THREAD"]
+  },
+  "FREYA_SSR": {
+    "id": "FREYA_SSR",
+    "name": "Freya (inspired)",
+    "rarity": "SSR",
+    "series": "Dan-like",
+    "tags": ["goddess","buffer","female_only"],
+    "path": "Support",
+    "base_stats": { "atk": 100, "hp": 820, "def": 65, "spd": 105, "crit": 0.15 },
+    "skills": ["CHARM_OF_VALHALLA","GLIMMERING_GAZE","EUPHORIC_CHORUS"]
+  },
+  "AZRYNN_SSR": {
+    "id": "AZRYNN_SSR",
+    "name": "Azrynn",
+    "rarity": "SSR",
+    "series": "Original",
+    "tags": ["fighter","female_only"],
+    "path": "DPS",
+    "base_stats": { "atk": 130, "hp": 750, "def": 55, "spd": 108, "crit": 0.20 },
+    "skills": ["RADIANT_COMBO","STARFALL_IMPACT","AURA_OVERDRIVE"]
+  },
+  "MIKASA_SR": {
+    "id": "MIKASA_SR",
+    "name": "Mika (sword scout inspired)",
+    "rarity": "SR",
+    "series": "Scout-like",
+    "tags": ["dps","female_only"],
+    "path": "DPS",
+    "base_stats": { "atk": 95, "hp": 680, "def": 50, "spd": 112, "crit": 0.12 },
+    "skills": ["TRI_SLASH","ANCHOR_SHIFT","RESOLVE"]
+  },
+  "RIN_R": {
+    "id": "RIN_R",
+    "name": "Rin (mage apprentice inspired)",
+    "rarity": "R",
+    "series": "Magus-like",
+    "tags": ["caster","female_only"],
+    "path": "Support",
+    "base_stats": { "atk": 60, "hp": 520, "def": 40, "spd": 100, "crit": 0.05 },
+    "skills": ["EMBER_SPARK","FOCUS_SIGIL","ARCANE_TETHER"]
+  }
+}
diff --git a/data/missions.json b/data/missions.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/data/missions.json
@@ -0,0 +1,28 @@
+[
+  {
+    "id": "MSN_001",
+    "name": "Lower Dungeon Sweep",
+    "level": 1,
+    "recommended_power": 1000,
+    "modifiers": ["beast_type_enemies","poison_hazards"],
+    "rewards": { "gold": [100,200], "fragments": ["SR_ANY:1"] }
+  },
+  {
+    "id": "MSN_002",
+    "name": "Temple of Threads",
+    "level": 3,
+    "recommended_power": 2000,
+    "modifiers": ["warded_enemies","blessing_fields"],
+    "rewards": { "gold": [300,450], "fragments": ["SSR_TARGETED:HESTIA_SSR:1"] }
+  }
+]
diff --git a/data/rates.json b/data/rates.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/data/rates.json
@@ -0,0 +1,11 @@
+{
+  "base": { "R": 0.82, "SR": 0.15, "SSR": 0.03 },
+  "soft_pity": { "start": 60, "step": 0.005, "cap": 0.8 },
+  "hard_pity": 90,
+  "rate_up": { "on_banner": 0.7 }
+}
diff --git a/scenes/ui/CardGrid.gd b/scenes/ui/CardGrid.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scenes/ui/CardGrid.gd
@@ -0,0 +1,29 @@
+extends Control
+
+@onready var name_lbl:Label = $Margin/Panel/VBox/Name
+@onready var rarity_lbl:Label = $Margin/Panel/VBox/Rarity
+@onready var series_lbl:Label = $Margin/Panel/VBox/Series
+
+func set_card(ch:Dictionary, rarity:String):
+    name_lbl.text = ch.get("name","?")
+    rarity_lbl.text = rarity
+    series_lbl.text = ch.get("series","")
+    modulate = _rarity_color(rarity)
+
+func _rarity_color(r:String)->Color:
+    match r:
+        "SSR": return Color(1, 0.92, 0.25)
+        "SR": return Color(0.7, 0.85, 1.0)
+        _: return Color(1,1,1)
diff --git a/scenes/ui/CardGrid.tscn b/scenes/ui/CardGrid.tscn
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scenes/ui/CardGrid.tscn
@@ -0,0 +1,65 @@
+[gd_scene load_steps=3 format=3 uid="uid://cardgrid"]
+
+[ext_resource type="Script" path="res://scenes/ui/CardGrid.gd" id="1_fa0sp"]
+
+[node name="CardGrid" type="Control"]
+layout_mode = 3
+anchors_preset = 15
+anchor_right = 1.0
+anchor_bottom = 1.0
+offset_left = 0.0
+offset_top = 0.0
+offset_right = 0.0
+offset_bottom = 0.0
+script = ExtResource("1_fa0sp")
+
+[node name="Margin" type="MarginContainer" parent="."]
+layout_mode = 1
+offset_left = 0.0
+offset_top = 0.0
+offset_right = 240.0
+offset_bottom = 320.0
+theme_override_constants/margin_left = 8
+theme_override_constants/margin_top = 8
+theme_override_constants/margin_right = 8
+theme_override_constants/margin_bottom = 8
+
+[node name="Panel" type="Panel" parent="Margin"]
+layout_mode = 2
+custom_minimum_size = Vector2(224, 304)
+
+[node name="VBox" type="VBoxContainer" parent="Margin/Panel"]
+layout_mode = 2
+offset_left = 8.0
+offset_top = 8.0
+offset_right = 216.0
+offset_bottom = 296.0
+theme_override_constants/separation = 6
+
+[node name="Placeholder" type="ColorRect" parent="Margin/Panel/VBox"]
+layout_mode = 2
+custom_minimum_size = Vector2(200, 240)
+color = Color(0.12, 0.12, 0.12, 1)
+
+[node name="Name" type="Label" parent="Margin/Panel/VBox"]
+layout_mode = 2
+text = "Name"
+autowrap_mode = 2
+
+[node name="Rarity" type="Label" parent="Margin/Panel/VBox"]
+layout_mode = 2
+text = "RAR"
+
+[node name="Series" type="Label" parent="Margin/Panel/VBox"]
+layout_mode = 2
+text = "Series"
diff --git a/scenes/ui/GachaView.gd b/scenes/ui/GachaView.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scenes/ui/GachaView.gd
@@ -0,0 +1,63 @@
+extends Control
+
+@onready var single_btn:Button = $Margin/VBox/HBox/Single
+@onready var ten_btn:Button = $Margin/VBox/HBox/Ten
+@onready var banner_opt:OptionButton = $Margin/VBox/Banner
+@onready var grid:GridContainer = $Margin/VBox/Results
+@onready var back_btn:Button = $Margin/VBox/Back
+
+var current_banner := "BNR_MULTIVERSE_MIX"
+
+func _ready() -> void:
+    _populate_banners()
+    single_btn.pressed.connect(_on_single)
+    ten_btn.pressed.connect(_on_ten)
+    back_btn.pressed.connect(func(): get_tree().change_scene_to_file("res://scenes/ui/MainMenu.tscn"))
+
+func _populate_banners():
+    banner_opt.clear()
+    var idx := 0
+    for b in DB.banners:
+        banner_opt.add_item("%s (%s)".sprintf([b.name, b.id]))
+        banner_opt.set_item_metadata(idx, b.id)
+        idx += 1
+    banner_opt.item_selected.connect(func(i): current_banner = banner_opt.get_item_metadata(i))
+    if idx > 0:
+        banner_opt.select(0)
+        current_banner = banner_opt.get_item_metadata(0)
+
+func _on_single() -> void:
+    var p := Gacha.pull_once(current_banner)
+    if p.is_empty(): return
+    GameState.apply_pull_results([p])
+    _show_results([p])
+
+func _on_ten() -> void:
+    var rs := Gacha.pull_x10(current_banner)
+    GameState.apply_pull_results(rs)
+    _show_results(rs)
+
+func _show_results(rs:Array) -> void:
+    for c in grid.get_children():
+        c.queue_free()
+    for r in rs:
+        var ch := DB.chars.get(r.id, null)
+        if ch == null: continue
+        var card := load("res://scenes/ui/CardGrid.tscn").instantiate()
+        card.set_card(ch, r.rarity)
+        grid.add_child(card)
diff --git a/scenes/ui/GachaView.tscn b/scenes/ui/GachaView.tscn
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scenes/ui/GachaView.tscn
@@ -0,0 +1,133 @@
+[gd_scene load_steps=3 format=3 uid="uid://gachaview"]
+
+[ext_resource type="Script" path="res://scenes/ui/GachaView.gd" id="1_wcc6k"]
+
+[node name="GachaView" type="Control"]
+layout_mode = 3
+anchors_preset = 15
+anchor_right = 1.0
+anchor_bottom = 1.0
+offset_left = 0.0
+offset_top = 0.0
+offset_right = 0.0
+offset_bottom = 0.0
+script = ExtResource("1_wcc6k")
+
+[node name="Margin" type="MarginContainer" parent="."]
+layout_mode = 1
+offset_left = 16.0
+offset_top = 16.0
+offset_right = 1264.0
+offset_bottom = 704.0
+theme_override_constants/margin_left = 16
+theme_override_constants/margin_top = 16
+theme_override_constants/margin_right = 16
+theme_override_constants/margin_bottom = 16
+
+[node name="VBox" type="VBoxContainer" parent="Margin"]
+layout_mode = 2
+theme_override_constants/separation = 12
+
+[node name="Title" type="Label" parent="Margin/VBox"]
+layout_mode = 2
+text = "Gacha"
+theme_override_font_sizes/font_size = 28
+
+[node name="Banner" type="OptionButton" parent="Margin/VBox"]
+layout_mode = 2
+size_flags_horizontal = 3
+
+[node name="HBox" type="HBoxContainer" parent="Margin/VBox"]
+layout_mode = 2
+theme_override_constants/separation = 12
+
+[node name="Single" type="Button" parent="Margin/VBox/HBox"]
+layout_mode = 2
+text = "Single Pull"
+
+[node name="Ten" type="Button" parent="Margin/VBox/HBox"]
+layout_mode = 2
+text = "10 Pull"
+
+[node name="ResultsLabel" type="Label" parent="Margin/VBox"]
+layout_mode = 2
+text = "Results"
+
+[node name="Results" type="GridContainer" parent="Margin/VBox"]
+layout_mode = 2
+columns = 4
+theme_override_constants/h_separation = 12
+theme_override_constants/v_separation = 12
+
+[node name="Back" type="Button" parent="Margin/VBox"]
+layout_mode = 2
+text = "← Back"
diff --git a/scenes/ui/MainMenu.gd b/scenes/ui/MainMenu.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scenes/ui/MainMenu.gd
@@ -0,0 +1,25 @@
+extends Control
+
+@onready var gacha_btn:Button = $Center/VBox/Gacha
+@onready var quit_btn:Button = $Center/VBox/Quit
+
+func _ready() -> void:
+    SaveManager.load_all()
+    gacha_btn.pressed.connect(func(): get_tree().change_scene_to_file("res://scenes/ui/GachaView.tscn"))
+    quit_btn.pressed.connect(_on_quit)
+
+func _on_quit() -> void:
+    SaveManager.save_all()
+    get_tree().quit()
diff --git a/scenes/ui/MainMenu.tscn b/scenes/ui/MainMenu.tscn
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scenes/ui/MainMenu.tscn
@@ -0,0 +1,78 @@
+[gd_scene load_steps=3 format=3 uid="uid://mainmenu"]
+
+[ext_resource type="Script" path="res://scenes/ui/MainMenu.gd" id="1_7a1qf"]
+
+[node name="MainMenu" type="Control"]
+layout_mode = 3
+anchors_preset = 15
+anchor_right = 1.0
+anchor_bottom = 1.0
+offset_left = 0.0
+offset_top = 0.0
+offset_right = 0.0
+offset_bottom = 0.0
+script = ExtResource("1_7a1qf")
+
+[node name="Center" type="CenterContainer" parent="."]
+layout_mode = 1
+offset_left = 0.0
+offset_top = 0.0
+offset_right = 1280.0
+offset_bottom = 720.0
+
+[node name="VBox" type="VBoxContainer" parent="Center"]
+layout_mode = 2
+theme_override_constants/separation = 12
+
+[node name="Title" type="Label" parent="Center/VBox"]
+layout_mode = 2
+text = "Divine Gacha (Minimal)"
+theme_override_font_sizes/font_size = 32
+horizontal_alignment = 1
+
+[node name="Gacha" type="Button" parent="Center/VBox"]
+layout_mode = 2
+text = "Start Gacha"
+custom_minimum_size = Vector2(240, 48)
+
+[node name="Quit" type="Button" parent="Center/VBox"]
+layout_mode = 2
+text = "Quit"
+custom_minimum_size = Vector2(240, 48)
diff --git a/scripts/BattleSimulator.gd b/scripts/BattleSimulator.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scripts/BattleSimulator.gd
@@ -0,0 +1,103 @@
+extends Node
+
+class Unit:
+    var id:String
+    var name:String
+    var stats := {"atk":0,"hp":0,"def":0,"spd":100,"crit":0.05}
+    var hp:int
+    var path:String
+    var team:int
+    func _init(d:Dictionary, team:int):
+        id = d.get("id","?")
+        name = d.get("name","Unit")
+        stats = d.get("base_stats", stats)
+        hp = int(stats.hp)
+        path = d.get("path","DPS")
+        self.team = team
+
+func simulate(team_a_ids:Array, team_b_power:int, god_mods:Array) -> Dictionary:
+    var team_a:Array[Unit] = []
+    for cid in team_a_ids:
+        if not DB.chars.has(cid): continue
+        var ch := DB.chars[cid]
+        team_a.append(Unit.new(ch, 0))
+    var team_b:Array[Unit] = _generate_enemy(team_b_power)
+
+    for gm in god_mods:
+        _apply_god_card(team_a, gm)
+
+    var rounds := 0
+    while rounds < 20 and _alive(team_a) and _alive(team_b):
+        var order := _speed_order(team_a, team_b)
+        for u in order:
+            if u.hp <= 0: continue
+            var target := _pick_target(u.team == 0 ? team_b : team_a)
+            if target == null: break
+            var dmg := max(1, int(u.stats.atk * 1.0) - int(target.stats.def * 0.3))
+            target.hp -= dmg
+        rounds += 1
+
+    var win := _alive(team_a) and not _alive(team_b)
+    var stars := win ? (rounds <= 10 ? 3 : (rounds <= 15 ? 2 : 1)) : 0
+    return { "win": win, "rounds": rounds, "stars": stars }
+
+func _alive(t:Array[Unit])->bool:
+    for u in t:
+        if u.hp > 0: return true
+    return false
+
+func _speed_order(a:Array[Unit], b:Array[Unit])->Array[Unit]:
+    var all := a.duplicate() + b.duplicate()
+    all.sort_custom(func(x,y): return x.stats.spd > y.stats.spd)
+    return all
+
+func _pick_target(team:Array[Unit])->Unit:
+    var alive := []
+    for u in team:
+        if u.hp > 0: alive.append(u)
+    if alive.is_empty(): return null
+    return alive[RNG.randi_range(0, alive.size()-1)]
+
+func _generate_enemy(power:int)->Array[Unit]:
+    var out:Array[Unit] = []
+    for i in 3:
+        var d = {
+            "id": "ENEMY_%s" % i,
+            "name": "Beast %s" % i,
+            "path": "DPS",
+            "base_stats": {"atk": 60 + power/15, "hp": 400 + power/3, "def": 30 + power/20, "spd": 95, "crit":0.05}
+        }
+        out.append(Unit.new(d, 1))
+    return out
+
+func _apply_god_card(team:Array[Unit], card:String) -> void:
+    match card:
+        "GOD_ATK_UP":
+            for u in team: u.stats.atk *= 1.2
+        "GOD_SHIELD":
+            for u in team: u.hp += 100
+        _:
+            pass
diff --git a/scripts/MissionResolver.gd b/scripts/MissionResolver.gd
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/scripts/MissionResolver.gd
@@ -0,0 +1,36 @@
+extends Node
+
+var sim := preload("res://scripts/BattleSimulator.gd").new()
+
+func run_mission(mission_id:String, team_key:String, god_cards:Array=[]) -> Dictionary:
+    var mission := {}
+    for m in DB.missions:
+        if m.get("id","") == mission_id:
+            mission = m
+            break
+    var team_ids:Array = GameState.teams.get(team_key, [])
+    var res := sim.simulate(team_ids, mission.get("recommended_power",1000), god_cards)
+    if res.get("win",false):
+        var gmin := int(mission.rewards.gold[0])
+        var gmax := int(mission.rewards.gold[1])
+        var g := RNG.randi_range(gmin, gmax)
+        GameState.currencies.gold += g
+    return res
--
2.44.0
